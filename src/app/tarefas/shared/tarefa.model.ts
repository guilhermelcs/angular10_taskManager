export class Tarefa {
    constructor( public id?: number,
                 public title?: string,
                 public descricao?: string,
                 public concluido?: boolean,
                ) {}
}